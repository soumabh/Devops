name: End-to-end-test
on: push
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: cypress run
        uses: cypress-io/github-action@v5